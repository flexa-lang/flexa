{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug flexa",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/debug/flexa",
      "args": ["-l", "${workspaceFolder}/std/src", "${workspaceFolder}/tests/main.flx"],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "setupCommands": [
        {
          "description": "Habilitar formatação para GDB",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
